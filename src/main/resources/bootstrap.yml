spring:
  application:
    name: ms-registry
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      username: admin
      password: nttdatacorazon
  config:
    import: optional:configserver:http://localhost:8888